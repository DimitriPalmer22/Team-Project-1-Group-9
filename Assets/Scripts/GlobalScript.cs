using UnityEngine;

public class GlobalScript : MonoBehaviour
{
    private bool _isHardcore;

    private bool _isInifiniteHealth;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
